---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";

const projects = (await Astro.glob("./project/*.{md,mdx}")).sort(
  (a, b) =>
    new Date(b.frontmatter.publishDate).valueOf() -
    new Date(a.frontmatter.publishDate).valueOf()
);

const favs = projects.filter((project) => {
  if (project.frontmatter.fav == true) {
    return project;
  }
});

const last_projects = favs.slice(0, 3);

const posts = (await Astro.glob("./blog/*.{md,mdx}")).sort(
  (a, b) =>
    new Date(b.frontmatter.pubDate).valueOf() -
    new Date(a.frontmatter.pubDate).valueOf()
);

const last_posts = posts.slice(0, 3);
---

<BaseLayout>
  <div class="pb-12 mt-5">
    <h1 class="text-5xl font-bold">Fernando García</h1>

    <h2 class="mt-3 text-3xl font-semibold">
      Frontend Developer - Web Insights Implementation
    </h2>
    <p class="mt-3 text-lg leading-8">
      Boost your project with innovative solutions in speed, security, and
      performance. I specialize in user interface development and
      creating scalable frontend solutions.
    </p>
    <p class="mt-3 text-lg leading-8">
      ✅ Scalable Frontend Development: Speed up your projects with scalability in mind, using a user-focused and continuous improvement approach.
    </p>
    <p class="mt-3 text-lg leading-8">
      📊 Collect Valuable Metrics: Gather relevant data and events from your project. Use it to your advantage as feedback to analyze and optimize the user experience and increase revenue.
    </p>
    <p class="mt-3 text-lg leading-8">
      💡  Take your product to the next level: Witness how your project transforms and reach its full potential with our services.
    </p>

    <div class="mt-5">
      <a
        class="btn cta"
        href="mailto:fer.garciabayon@gmail.com"
        target="_blank"
      >
        Let's Connect!</a
      >
      <a href="/cv" class="btn btn-outline ml-5"> About Me</a>
    </div>
  </div>

  <!-- <div>
    <div class="text-3xl w-full font-bold mb-2">Latest from Portfolio {"</>"}</div>
  </div>

  {
  last_projects.map((project) => (
  <HorizontalCard title={project.frontmatter.title} img={project.frontmatter.heroImage}
    desc={project.frontmatter.description} badge={project.frontmatter.badge} url={project.url} target="_self" />
  <div class="divider my-0"></div>
  ))
  } -->

  <div>
    <div class="text-3xl w-full font-bold mb-5 mt-6">Latest from blog</div>
  </div>

  {
    last_posts.map((post) => (
      <>
        <HorizontalCard
          title={post.frontmatter.title}
          img={post.frontmatter.heroImage}
          desc={post.frontmatter.description}
          badge={post.frontmatter.badge}
          url={post.url}
          target="_self"
        />
        <div class="divider my-0" />
      </>
    ))
  }
</BaseLayout>
