---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";

const projects = (await Astro.glob("./project/*.{md,mdx}")).sort(
  (a, b) =>
    new Date(b.frontmatter.publishDate).valueOf() -
    new Date(a.frontmatter.publishDate).valueOf()
);

const favs = projects.filter((project) => {
  if (project.frontmatter.fav == true) {
    return project;
  }
});

const last_projects = favs.slice(0, 3);

const posts = (await Astro.glob("./blog/*.{md,mdx}")).sort(
  (a, b) =>
    new Date(b.frontmatter.pubDate).valueOf() -
    new Date(a.frontmatter.pubDate).valueOf()
);

const last_posts = posts.slice(0, 3);
---

<BaseLayout>
  <div class="pb-12 mt-5">
    <h1 class="text-5xl font-bold">Fernando García</h1>

    <h2 class="mt-3 text-3xl font-semibold">Frontend Developer</h2>
    <p class="p-3 text-lg leading-8">
      💻 I'm a frontend developer passionate about speed, security and
      performance metrics. I specialize in user interface development for
      creating scalable frontend solutions.
    </p>
    <p class="p-3 text-lg leading-8 hover:bg-slate-800 hover:rounded-md">
      ✅ Frontend Development: Speed up your projects with scalability in mind,
      using continuous improvement.
    </p>
    <!-- <p class="p-3 text-lg leading-8 hover:bg-slate-800 hover:rounded-md ">
      🤖 Content Automation System: Save time and resources removing repetitive tasks, and focus on creating high-quality content only.
    </p> -->
    <p class="p-3 text-lg leading-8 hover:bg-slate-800 hover:rounded-md">
      📊 Collecting Metrics: Obtain relevant data from your project. Use it as
      feedback to optimize the user experience.
    </p>
    <p class="mt-3 p-3 text-lg leading-8">
      💡 Take your project to the next level.
    </p>

    <div class="mt-5">
      <a
        class="btn cta"
        href="mailto:fer.garciabayon@gmail.com"
        target="_blank"
      >
        Let's Connect!</a
      >
      <a href="/cv" class="btn btn-outline ml-5"> About Me</a>
    </div>
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-5 mt-6">Latest from blog</div>
  </div>

  {
    last_posts.map((post) => (
      <>
        <HorizontalCard
          title={post.frontmatter.title}
          img={post.frontmatter.heroImage}
          desc={post.frontmatter.description}
          badge={post.frontmatter.badge}
          url={post.url}
          target="_self"
        />
        <div class="divider my-0" />
      </>
    ))
  }

  
<div>
  <div class="text-3xl w-full font-bold mb-2 mt-10">
    Latest from Portfolio
  </div>
</div>

{
  last_projects.map((project) => (
    <>
      <HorizontalCard
        title={project.frontmatter.title}
        img={project.frontmatter.heroImage}
        desc={project.frontmatter.description}
        badge={project.frontmatter.badge}
        url={project.url}
        target="_self"
      />
      <div class="divider my-0" />
    </>
  ))
}
</BaseLayout>
